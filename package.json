{
  "name": "prisma-sqlmodel-gen",
  "version": "0.1.0",
  "description": "Generate SQLModel table models from a Prisma schema",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "prisma",
    "sqlmodel",
    "python",
    "typescript",
    "codegen",
    "postgresql",
    "mysql"
  ],
  "main": "./dist/cli.cjs",
  "module": "./dist/cli.js",
  "types": "./dist/cli.d.ts",
  "bin": {
    "prisma-sqlmodel-gen": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/cli.d.ts",
      "import": "./dist/cli.js",
      "default": "./dist/cli.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist coverage",
    "build": "tsup",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:integration": "npm run build && vitest run --config vitest.integration.config.ts",
    "test:all": "npm run check && npm test && npm run test:integration",
    "prepublishOnly": "npm run clean && npm run test:all && npm run build"
  },
  "dependencies": {
    "@mrleebo/prisma-ast": "^0.15.0",
    "@prisma/generator-helper": "^7.7.0",
    "@prisma/internals": "^7.7.0"
  },
  "devDependencies": {
    "@prisma/adapter-mariadb": "^7.7.0",
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7.7.0",
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^3.2.4",
    "mariadb": "^3.4.5",
    "pg": "^8.16.3",
    "prisma": "^7.7.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
